[project]
name = "excel-to-pdf-converter"
version = "1.0.0"
dependencies = [
    "openpyxl==3.1.2",
    "reportlab==4.0.7",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
